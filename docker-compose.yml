services:
  todo-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - todo-data:/data
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped

volumes:
  todo-data:
